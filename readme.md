## Parse catalogues for classical music works.

- upload docs to gcp bucket
- change the file name in batch_process.py + run
- download the output
- change the file name in json2md_splitter.py + run
- run work_chunk_splitter.python (uses llm)